# ☕ Coffee Shop — React + Vite

Aplicação web que simula uma loja de cafés: catálogo com busca, sacola/carrinho e checkout.

## ✅ Rodando o projeto

Pré-requisitos: **Node.js** (recomendado 18+).

```bash
npm install
npm run dev
```

Outros comandos:

- `npm run build`
- `npm run preview`
- `npm run lint`

## 📦 Dados

- Produtos: `public/api/products.json`
- Envio do pedido: `POST` para `https://jsonplaceholder.typicode.com/posts` (endpoint de teste)
